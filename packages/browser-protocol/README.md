# Browser Protocol

Shared OMP browser feedback protocol types and runtime validators.

The package is used by the browser broker, Chrome extension, and coding-agent integration so all routing payloads share one versioned contract.
