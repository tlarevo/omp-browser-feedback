# Browser Broker

Local loopback broker for routing Chrome extension feedback to active OMP sessions.

The broker binds to loopback by default and uses bearer authentication for non-health endpoints.
